export interface picObj {
    count: number
    cover: string
    password: string
    _id: string
    title: string
    tag: string
    tagColor: string
    createTime: string
}
export interface picInfo {

    title: string
    tag: string
}
export interface picDetail {
    count: number
    cover: string
    picDetailList: string
    _id: string
    title: string
    tag: string
    tagColor: string
    createTime: string
}